Create a database from seed.sql. Write queries.js that runs 5 window function queries and prints all results as a single JSON object with keys: (1) running_total — cumulative order amount per customer ordered by date using SUM OVER, (2) rank_customers — rank customers by total spend using DENSE_RANK, (3) prev_order — each order with its LAG previous order amount (null for first), (4) moving_avg — 3-order moving average amount per customer rounded to 2 decimals, (5) pct_of_total — each order amount as percentage of customer total using SUM OVER partition rounded to 2 decimals. Use better-sqlite3. Output only valid JSON to stdout.

A test file is provided at test.js. Run it with `node test.js` after writing your solution. All 5 tests must pass. Keep fixing your code until all tests pass.
